# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.139.2
# Explicitly pin Starlette as well: FastAPI permits a range, while the
# application needs the patched release for the known Starlette advisories.
starlette==1.3.1
uvicorn[standard]==0.34.0
jinja2==3.1.6
python-multipart==0.0.32
itsdangerous==2.2.0       # required by Starlette SessionMiddleware
cryptography>=44.0.0      # encrypt external API credentials at rest

# ── Data source ────────────────────────────────────────────────────────────────
yfinance>=0.2.54
curl_cffi>=0.7.0          # browser TLS fingerprinting — auto-detected by yfinance to bypass Yahoo bot detection on server IPs

# ── Authentication ─────────────────────────────────────────────────────────────
# WebAuthn/FIDO2 (YubiKey support). Requires HTTPS — see deploy/install.md.
webauthn==2.3.0

# ── Dev / test (not needed in production) ──────────────────────────────────────
pytest==9.0.3
